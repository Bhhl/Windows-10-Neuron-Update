code

if (Оптимизация операционной системы Windows NT)
else (Сохранение данных)
if (Оптимизация ядра Windows NT)
if (Оптимизация оболочки Windows NT)
	else (Сохранение данных)
if (Исправление меню пуск Windows NT)
	else (Сохранение данных)
if (Исправление открытия меню пуск Windows NT)
	else (Сохранение данные)
if (Оптимизация интернет скорости)
	else (Сохранение данных)
if (Оптимизация интернет соединения)
	else (Сохранение данных)
if (Оптимизация интернет передачи данных)
	else (Сохранение данных)
if (Оптимизация интернет скорости соединения)
	else (Сохранение данных)
if (Создание виртуального интернет провайдера СоветТелеком)
	else (Сохранение данных)
if (Оптимизация виртуального интернет провайдера СоветТелеком)
	else (Сохранение данных)
if (Контроль качества соединения виртуального интернет провайдера СоветТелеком)
	else (Сохранение данных)
if (Улучшение качества сети интернет виртуального провайдера СоветТелеком)
	else (Сохранение данных)
if (Поддержка терабитной скорости интернет виртуального провайдера СоветТелеком)
	else (Сохранение данных)
if (Поддержка ультра высокого соединения интернет виртуального провайдера СоветТелеком)
	else (Сохранение данных)
if (Поддержка низкой интернет нагрузки)
	else (Сохранение данных)
if (Поддержка ультра высокой производительности CPU)
	else (Сохранение данных)
if (Поддержка ультра высокой производительности GPU)
	else (Сохранение данных)
if (Поддержка ультра высокой производительности RAM)
	else (Сохранение данных)
copydetails
endcode
